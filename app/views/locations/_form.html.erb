<%= form_for(@location) do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class= "field">
  <%= f.label :name %></br>
  <%= f.text_field :name %>
</div>
<div>
  <%= f.label :country %></br>
  <%= f.text_field :country %>
<div>
<div>
  <%= f.label :region %></br>
  <%= f.text_field :region %>
</div>
</br>
<label>Good Months to Visit</label>
<% @months.each do |month| %>
  <div class="checkbox">
      <label>
       <%= check_box_tag "location[month_ids][]", month.id %> <%= month.name %>
     </label>
  </div>
<% end %>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
